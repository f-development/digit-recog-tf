name: pr
on:
  - pull_request

permissions:
  id-token: write
  actions: write
  contents: read
  pull-requests: read

jobs:
  plan:
    runs-on: ubuntu-latest
    if: |
      !contains(github.event.pull_request.labels.*.name, 'terraform: apply')
    name: plan
    steps:
      - uses: actions/checkout@v3
      - uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          role-to-assume: arn:aws:iam::721756646069:role/f-development-digit-recog-tf-github-actions-terraform
          aws-region: us-east-1
      - uses: google-github-actions/auth@v1
        with:
          workload_identity_provider: projects/287979390947/locations/global/workloadIdentityPools/ga-3-f-development-digit-recog-tf-tf/providers/provider
          service_account: github-actions-terraform@f-development.iam.gserviceaccount.com
      - uses: hashicorp/setup-terraform@v2.0.3
      - run: >
          terraform init
          -backend-config=bucket=f-development-digit-recog-tf-state
          -backend-config=dynamodb_table=f-development-digit-recog-tf-state-lock
      - run: |
          terraform plan -lock-timeout=1h \
          -var=role_arn=arn:aws:iam::721756646069:role/f-admin-terraform \
          -var=impersonate_sa=terraform@f-development.iam.gserviceaccount.com
  apply:
    runs-on: ubuntu-latest
    if: |
      contains(github.event.pull_request.labels.*.name, 'terraform: apply')
    name: apply
    steps:
      - uses: actions/checkout@v3
      - uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          role-to-assume: arn:aws:iam::721756646069:role/f-development-digit-recog-tf-github-actions-terraform
          aws-region: us-east-1
      - uses: google-github-actions/auth@v1
        with:
          workload_identity_provider: projects/287979390947/locations/global/workloadIdentityPools/ga-3-f-development-digit-recog-tf-tf/providers/provider
          service_account: github-actions-terraform@f-development.iam.gserviceaccount.com
      - uses: hashicorp/setup-terraform@v2.0.3
      - run: "terraform version"
      - run: >
          terraform init
          -backend-config=bucket=f-development-digit-recog-tf-state
          -backend-config=dynamodb_table=f-development-digit-recog-tf-state-lock
      - run: |
          terraform apply -auto-approve -lock-timeout=1h \
          -var=role_arn=arn:aws:iam::721756646069:role/f-admin-terraform \
          -var=impersonate_sa=terraform@f-development.iam.gserviceaccount.com